[project]
name = "madrugascanner"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "nico-madruga", email = "nizinhoms@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
madrugascanner = "madrugascanner:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
